import {Component} from '@angular/core'; // 2. import a component from the core

@Component({
  selector: 'app-header',
  templateUrl: './header.component.html'
})// 1.put this "@component decorator"// 3. add js object to tjis decorator to configure this component
/*
 3. import them inside the app module
*/
export class HeaderComponent{

}
